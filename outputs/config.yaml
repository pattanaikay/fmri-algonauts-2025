seed: 42
device: cuda
out_dir: outputs
overwrite: true
lr: 0.0001
weight_decay: 0.01
epochs: 100
batch_size: 32
model:
  hidden_model: transformer
  embed_dim: 768
  global_pool: avg
datasets_root: data
